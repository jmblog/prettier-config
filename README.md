# @jmblog/prettier-config

## Installation

```bash
$ npm install jmblog/prettier-config --save-dev
```

## Usage

Import `@jmblog/prettier-config` in the `prettier.config.js` file.

```bash
module.exports = require('@jmblog/prettier-config');
```
